#include <iostream>
#include <vector>
#include <algorithm>
#include<cmath>
using namespace std;
int main(){
    int t;
    cin>>t;
    while(t--){
        int n ;
        cin>>n;
        long long sum=0;
        for(int i=0;i<n;i++){
            int b;
            cin>>b;
            sum+=b;
        }
        double c=pow(sum,0.5);
        if(c-(int)c==0) cout<<"YES"<<endl;
        else cout<<"NO"<<endl;
        
    }
    return 0;
 
 
}