#include <iostream>
#include <vector>
#include<set>
using namespace std;
int main() {
    int t;
    cin >> t;
    while(t--){
        set<int>s;
        int a,b,c,d;
        cin>>a>>b>>c>>d;
        s.insert({a,b,c,d});
        if(s.size()==1){
            cout<<"YES"<<endl;
        }
        else{
            cout<<"NO"<<endl;
        }
    }
 
    return 0;
}