#include <iostream>
#include <vector>
#include<algorithm>
using namespace std;
int main() {
    int t;
    cin >> t;
    while(t--){
        string a;
        cin>>a;
        int c=std::stoi(a.substr(0,1));
        int d=std::stoi(a.substr(2,1));
        cout<<c+d<<endl;
    }
 
    return 0;
}